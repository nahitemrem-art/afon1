{
  "name": "project",
  "version": "1.0.0",
  "description": "Project with SQLite database",
  "main": "index.js",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "db:init": "node scripts/init-db.js"
  },
  "dependencies": {
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "better-sqlite3": "^9.0.0"
  }
}
